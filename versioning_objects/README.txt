This is of course not the schema for an application.

This directory contains the up-to-date version of the versioning functions we
use, and will be used to deploy new versions in all databases (by just linking
them in the version we want to deploy them in)
